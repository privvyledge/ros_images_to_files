"""
An example node that subscribes to n images and n camera infos with configurable QoS using image transport (and message filters)
Features:
 * subscribes to images with configurable QoS
 * supports compressed images
 * support automatic subscription to camera info
 * supports optional hardware encoding
"""